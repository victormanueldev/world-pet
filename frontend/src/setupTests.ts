/**
 * setupTests.ts
 *
 * Automatically imported before each test suite (configured in vitest.config.ts).
 * Extends Vitest's expect with jest-dom matchers for DOM assertions.
 */
import "@testing-library/jest-dom";
